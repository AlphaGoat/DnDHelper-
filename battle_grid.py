import numpy as np
import pygame, sys

class BattleGrid(object):

    def __init__(self, dim_x, dim_y):
        '''initializes the battle grid. Each square represents
           5 ft. of distance in the battle space

                param dim_x: width of the battle grid
                param dim_y: height of the battle grid
        '''
        self.dim_x = dim_x
        self.dim_y = dim_y


        



